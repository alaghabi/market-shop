{
    auto_https off
}

{$SERVER_NAME:localhost} {
    @keycloak host auth.localhost
    handle @keycloak {
        reverse_proxy keycloak:8080
    }

    handle {
        root * /app/public
        encode zstd br gzip
        php_server
    }
}
